import 'package:flutter/material.dart';

class AlWirdPage extends StatelessWidget {
  const AlWirdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("الورد"));
  }
}
